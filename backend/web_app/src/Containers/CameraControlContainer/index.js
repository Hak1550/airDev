import React from 'react';
import CameraControlPage from '../../Pages/CameraControlPage';

const CameraControlContainer = () => {
  return <CameraControlPage />;
};

export default CameraControlContainer;
